<!DOCTYPE html>
<html><head><title>New Conversation</title><style>body{font-family:Arial;background:#f4f7f4;margin:0}.navbar{background:#1b5e20;color:white;padding:18px 40px;display:flex;justify-content:space-between}.navbar a{color:white;text-decoration:none;margin-left:15px;font-weight:bold}.container{max-width:700px;background:white;margin:40px auto;padding:25px;border-radius:10px;box-shadow:0 3px 10px rgba(0,0,0,.1)}input,textarea{width:100%;padding:10px;margin:8px 0;border:1px solid #ccc;border-radius:6px;box-sizing:border-box}.user-box{padding:8px;border-bottom:1px solid #eee}button{background:#2e7d32;color:white;border:0;padding:10px 14px;border-radius:6px;cursor:pointer}</style></head><body>
<div class="navbar"><h2>New Conversation</h2><div><a href="{{ route('messages.index') }}">Back</a></div></div>
<div class="container"><h1>Create Message / Group</h1>@if($errors->any())<div style="color:red"><ul>@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ route('messages.store') }}" enctype="multipart/form-data">@csrf<label>Group Name (optional)</label><input name="name" placeholder="Example: Team Alpha Cleanup Group"><label>Select members</label>@foreach($users as $user)<div class="user-box"><label><input type="checkbox" name="user_ids[]" value="{{ $user->id }}"> {{ $user->name }} ({{ $user->role }}) - {{ $user->email }}</label></div>@endforeach<label>First message</label><textarea name="body" placeholder="Write a message..."></textarea><label>Upload image (optional)</label><input type="file" name="photo" accept="image/*"><button type="submit">Create Conversation</button></form></div></body></html>
